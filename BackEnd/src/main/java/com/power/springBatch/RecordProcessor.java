package com.power.springBatch;

import org.springframework.batch.item.ItemProcessor;

import com.power.models.Record;

public class RecordProcessor //implements ItemProcessor<Record,Record>
{
	/*
	 * public Record process(Record record) throws Exception{
	 * System.out.println("Current record being processed: " +
	 * record.getPower_usage());
	 * 
	 * record.setCost(record.getCost().replace("$",""));
	 * record.setAccount_number("1"); return record; }
	 */
}
